<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="A collaboration tool">
    <meta name="author" content="Alexander Fuchsberger">
    <%= tag :meta, name: "user_token", content: Map.get @conn.assigns, :user_token, "" %>

    <title>Idea Nexus</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.9/css/all.css" integrity="sha384-5SOiIsAziJl6AWe0HWRKTXlfcSHKmYV4RBF18PPJ173Kzn7jzMyFuTtk8JA7QQG1" crossorigin="anonymous">
    <link rel="icon" type="image/x-icon" href="<%= Routes.static_path(@conn, "/images/favicon.ico") %>" >

    <!-- Google Analytics -->
    <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

    ga('create', '<%= ga_code() %>', 'auto');
    ga('send', 'pageview');
    </script>
    <!-- End Google Analytics -->

    <script>
      window.condition = <%= user_condition(@conn) %>;
      window.time_passed = <%= time_passed(@conn) %>;
      window.user = JSON.parse("<%= get_user(@conn) %>".replace(/(&quot\;)/g,"\""));
      console.log(window.user)
    </script>
    <script src="//cdn.tinymce.com/4/tinymce.min.js"></script>
    <script src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body data-js-view-name="<%= js_view_name(@conn, @view_template) %>">
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <%= home_button(@conn) %>
        <%= if admin?(@conn) do %>
          <ul class="navbar-nav mr-auto">
            <%= nav_item @conn, "Topics", Routes.topic_path(@conn, :index) %>
            <%= nav_item @conn, "New Topic", Routes.topic_path(@conn, :new) %>
            <%= nav_item @conn, "Users", Routes.user_path(@conn, :index) %>
            <%= nav_item @conn, "Participants", Routes.user_path(@conn, :participants) %>
          </ul>
        <% end %>

        <%= if @current_user do %>
          <form class="form-inline">
            <%= if admin?(@conn) do %>
              <%= logout_button(@conn) %>
            <% else %>
              <%= timer_button(@conn) %>
              <%= abort_button(@conn) %>
            <% end %>
          </form>
        <% end %>
      </div>
    </nav>
    <div class="container">
      <%= render "flash.html", type: :info, msg: get_flash(@conn, :info) %>
      <%= render "flash.html", type: :error, msg: get_flash(@conn, :error) %>
      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div>
  </body>
</html>
